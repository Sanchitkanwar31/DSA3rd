#include<stdio.h>
#include<math.h>
void main(){
//2*****Greatest among three****
    /*int a=5,b=2,c=6;
    if(a>b && a>c){
        printf("A is greatest %d",a);
    }
    else if(c>b && c>a){
        printf("C is greatest %d",c);

    }
    else{
        printf("B is greatest %d",b);
    }*/

//1*******Salary********
   /*int basic;

   printf("Enter basic salary : ");
   scanf("%d", &basic);

    int allowance;
    double hra, da, pf;
    char grade;
    scanf("%c",&grade);
 
    hra = 0.2 * basic;
    da = 0.5 * basic;
    pf = 0.11 * basic;
 
    // Condition to compute the
    // allowance for the person
    if (grade == 'A') {
        allowance = 1700;
    }
    else if (grade == 'B') {
        allowance = 1500;
    }
    else {
        allowance = 1300;
    }
    int gross;
 
    // Calculate gross salary
    gross =basic + hra + da + allowance - pf;
    printf("salary %d:",gross);*/

//3*****Vowel****
    /*char s;
    printf("Enter any alphabet in lowercased: \n");
    scanf("%s",&s);
    if(s=='a'||s=='u'||s=='o'||s=='i'||s=='e'){
        printf("It is a vowel");
    }
    else 
        printf("This is a consonant");
    */

//4*****Leap Year****
    /*int year;
    printf("Enter year");
    scanf("%d",&year);
    if(year%4==0&&year%400==0&&year%100!=0){
        printf("It is a leap year");
    }
    else    
        printf("Not a leap year");*/
    
//5****Triangle*****

   /*int side1, side2, side3;
   printf("Enter sides of triangle:");
   scanf("%d%d%d",&side1,&side2,&side3);
   if(side1 == side2 && side2 == side3)
      printf("The Given Triangle is equilateral");
   else if(side1 == side2 || side2 == side3 || side3 == side1)
      printf("The given Triangle is isosceles");
   else
      printf("The given Triangle is scalene");*/

//6****Termperature***
 
    /* int tmp;

    printf("Input days temperature : ");
    scanf("%d",&tmp);
   if(tmp<0)
             printf("Freezing weather.\n");
   else if(tmp<10)
            printf("Very cold weather.\n");
            else if(tmp<20)
                        printf("Cold weather.\n");
                    else if(tmp<30)
                               printf("Normal in temp.\n");
                            else if(tmp<40)
                                         printf("Its Hot.\n");
                                    else
                                           printf("Its very hot.\n");*/

//8*****Coordinate system*******

    /*int x,y;
    printf("Enter x and y:\n");
    scanf("%d %d",&x,&y);
    if(x>=0 && y>=0)
        printf("Lie in First quad");
    else if(x<=0 && y>=0)
        printf("Lie in Second quad");
    else if(x<=0 && y<=0)
        printf("Lie in Third quad");
    else 
        printf("Lie in Fourth quad");*/

//10*****Medclaim********
    
    


}
